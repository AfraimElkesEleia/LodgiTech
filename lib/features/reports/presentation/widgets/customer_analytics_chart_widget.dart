import 'package:flutter/material.dart';

class CustomerAnalyticsChartWidget extends StatelessWidget {
  const CustomerAnalyticsChartWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}